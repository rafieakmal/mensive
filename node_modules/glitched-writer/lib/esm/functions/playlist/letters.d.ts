import GlitchedWriter from '../..';
import { PlayOptions } from '../../types';
export default function prepLettersPlaylist(this: GlitchedWriter, playList: Promise<boolean>[], playOptions?: PlayOptions): void;
